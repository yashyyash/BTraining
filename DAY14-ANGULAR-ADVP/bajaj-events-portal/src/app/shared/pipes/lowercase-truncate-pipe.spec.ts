import { LowercaseTruncatePipe } from './lowercase-truncate-pipe';

describe('LowercaseTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new LowercaseTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
