import { LowecaseTruncPipe } from './lowecase-trunc-pipe';

describe('LowecaseTruncPipe', () => {
  it('create an instance', () => {
    const pipe = new LowecaseTruncPipe();
    expect(pipe).toBeTruthy();
  });
});
